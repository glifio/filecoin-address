/* eslint-env mocha */
const { expect } = require('chai')

describe('address', () => {
  describe('decode', () => {
    it('should decode ID address', async () => {
      expect(true).to.be.eql(true)
    })
  })
})
